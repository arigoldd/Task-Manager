<h1><%= @task.name %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%# Maybe we can add the if here %>

<%= link_to "Delete #{@task.name}",
            task_path,
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<%= link_to "Return", tasks_path %>
